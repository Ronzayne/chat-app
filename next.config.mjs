/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      { hostname: "fearless-bandicoot-658.convex.cloud" },
      { hostname: "fearless-bandicoot-658.convex.cloud" },
    ],
  },
};

export default nextConfig;
